<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Special "app op" permission (not a runtime dangerous permission): lets MATE
         read which app is currently in the foreground so it can detect when a
         guarded app is opened. The user must explicitly enable this in
         Settings > Apps > Special access > Usage access; declaring it here does
         not grant it. This is the standard, lower-risk alternative to an
         AccessibilityService for this purpose. -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />
    <!-- Lets the foreground watcher service (below) keep running reliably while
         it watches for a guarded app coming to the foreground. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- Needed on Android 13+ for the watcher's ongoing "MATE is watching" notification
         to be visible; the service still runs even if this is denied. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Android blocks an app from starting a new full-screen Activity while it has
         no visible window ("background activity launch" restrictions) — which is
         exactly the situation the watcher service is normally in. This permission
         lets the service briefly show a native overlay right before launching the
         intervention screen, which satisfies Android's "has a visible window"
         exemption. See ForegroundWatcherService.showTransitionCover(). -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- Lets the watcher resume itself after a device reboot (see
         BootCompletedReceiver) instead of silently staying off until the
         user happens to reopen the app. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <application
        android:label="MATE"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Full-screen "pause and reflect" screen shown on top of a guarded app the
             instant it's detected. Its own task (taskAffinity) and singleTask launch
             mode keep it from piling up or merging into MainActivity's task, and it's
             hidden from the recents list since it's only ever shown for a few seconds. -->
        <activity
            android:name=".InterventionActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:taskAffinity="com.example.mate.intervention"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode" />
        <!-- Polls for the foreground app changing so MATE can react even while its
             own UI isn't open. See MainActivity.kt / ForegroundWatcherService.kt for
             why this uses UsageStatsManager rather than an AccessibilityService. -->
        <service
            android:name=".ForegroundWatcherService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Detects when a user-chosen app is opened so MATE can show a brief pause-and-reflect screen before it opens. No data leaves the device." />
        </service>
        <!-- Restarts the watcher after a reboot using the last-synced state cached in
             NativeStateStore — see BootCompletedReceiver's doc comment. -->
        <receiver
            android:name=".BootCompletedReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
